# 🏗️ Arquitetura do Sistema

## Visão Geral

O Multi Lives é construído em uma arquitetura modular com separação clara entre frontend e backend, usando microserviços e cache distribuído.

## Componentes Principais

### 1. Frontend (Next.js + React)
```
┌─────────────────────────────────────────────┐
│  Client Application Layer                   │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ Pages (App Router)                  │   │
│  │ • /page.tsx (Home)                  │   │
│  │ • Dynamic routing                   │   │
│  └─────────────────────────────────────┘   │
│           ↓                                 │
│  ┌─────────────────────────────────────┐   │
│  │ Components                          │   │
│  │ • AuthForm  ┐                       │   │
│  │ • MultiPlayer ├─ Presentation      │   │
│  │ • LiveList   ┘                      │   │
│  │ • RoomList, etc                     │   │
│  └─────────────────────────────────────┘   │
│           ↓                                 │
│  ┌─────────────────────────────────────┐   │
│  │ State Management (Zustand)          │   │
│  │ • useAuthStore (user, token)        │   │
│  │ • useLiveStore (lives, rooms)       │   │
│  └─────────────────────────────────────┘   │
│           ↓                                 │
│  ┌─────────────────────────────────────┐   │
│  │ HTTP Client (Axios)                 │   │
│  │ • apiClient.ts                      │   │
│  │ • Request/Response interceptors     │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
        ↓ HTTP/REST
    TCP:3001
```

### 2. Backend (NestJS)
```
┌─────────────────────────────────────────────────┐
│  API Layer (NestJS)                             │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────────────────────────────────┐   │
│  │ Controllers (HTTP Endpoints)             │   │
│  │ ├─ AuthController                        │   │
│  │ │  ├─ POST /auth/register               │   │
│  │ │  └─ POST /auth/login                  │   │
│  │ ├─ UsersController                       │   │
│  │ │  ├─ GET /users                        │   │
│  │ │  └─ POST /users                       │   │
│  │ └─ LivesController                       │   │
│  │    ├─ GET /lives                        │   │
│  │    ├─ POST /lives (protected)           │   │
│  │    ├─ PUT /lives/:id (protected)        │   │
│  │    ├─ DELETE /lives/:id (protected)     │   │
│  │    └─ Rooms operations                  │   │
│  └──────────────────────────────────────────┘   │
│           ↓                                     │
│  ┌──────────────────────────────────────────┐   │
│  │ Middleware & Guards                      │   │
│  │ ├─ JwtGuard (protege rotas)              │   │
│  │ ├─ ValidationPipe (valida DTOs)          │   │
│  │ └─ CORS                                  │   │
│  └──────────────────────────────────────────┘   │
│           ↓                                     │
│  ┌──────────────────────────────────────────┐   │
│  │ Services (Business Logic)                │   │
│  │ ├─ AuthService                           │   │
│  │ │  ├─ validateUser()                    │   │
│  │ │  ├─ login()                           │   │
│  │ │  └─ register()                        │   │
│  │ ├─ UsersService                          │   │
│  │ │  ├─ create()                          │   │
│  │ │  └─ findOne()                         │   │
│  │ └─ LivesService                          │   │
│  │    ├─ createLive()                      │   │
│  │    ├─ findAllLives()                    │   │
│  │    ├─ createRoom()                      │   │
│  │    └─ addLiveToRoom()                   │   │
│  └──────────────────────────────────────────┘   │
│           ↓                                     │
│  ┌──────────────────────────────────────────┐   │
│  │ Data Access Layer (TypeORM)              │   │
│  │ ├─ UserRepository                        │   │
│  │ ├─ LiveRepository                        │   │
│  │ └─ RoomRepository                        │   │
│  └──────────────────────────────────────────┘   │
│           ↓                ↓                    │
│           │                │                    │
└───────────┼────────────────┼───────────────────┘
            │                │
        TCP:5432         TCP:6379
```

### 3. Banco de Dados (PostgreSQL)
```
┌────────────────────────────────┐
│ PostgreSQL Database            │
├────────────────────────────────┤
│                                │
│  ┌──────────────────────────┐  │
│  │ users table              │  │
│  │ ├─ id (PK, UUID)         │  │
│  │ ├─ email (UNIQUE)        │  │
│  │ ├─ username              │  │
│  │ ├─ password (hashed)     │  │
│  │ ├─ avatar                │  │
│  │ ├─ createdAt             │  │
│  │ └─ updatedAt             │  │
│  └──────────────────────────┘  │
│                                │
│  ┌──────────────────────────┐  │
│  │ lives table              │  │
│  │ ├─ id (PK, UUID)         │  │
│  │ ├─ title                 │  │
│  │ ├─ description           │  │
│  │ ├─ url                   │  │
│  │ ├─ thumbnail             │  │
│  │ ├─ isActive              │  │
│  │ ├─ viewers (counter)     │  │
│  │ ├─ creatorId (FK)        │  │
│  │ ├─ createdAt             │  │
│  │ └─ updatedAt             │  │
│  └──────────────────────────┘  │
│                                │
│  ┌──────────────────────────┐  │
│  │ rooms table              │  │
│  │ ├─ id (PK, UUID)         │  │
│  │ ├─ name                  │  │
│  │ ├─ description           │  │
│  │ ├─ creatorId (FK)        │  │
│  │ ├─ liveIds (UUID array)  │  │
│  │ ├─ viewers (counter)     │  │
│  │ └─ createdAt             │  │
│  └──────────────────────────┘  │
│                                │
└────────────────────────────────┘
```

### 4. Cache Layer (Redis)
```
┌──────────────────────────────┐
│ Redis Cache                  │
├──────────────────────────────┤
│                              │
│  Keys Pattern:               │
│  ├─ live:{id}                │
│  │  └─ {Live JSON}           │
│  │                            │
│  ├─ room:{id}                │
│  │  └─ {Room JSON}           │
│  │                            │
│  └─ session:{token}          │
│     └─ {User data}           │
│                              │
│  TTL: Configurável          │
│  Estratégia: LRU            │
│                              │
└──────────────────────────────┘
```

## Fluxo de Requisição

### Login Flow
```
1. User submits email + password
   ↓
2. Frontend: apiClient.login(email, password)
   ↓
3. Backend: POST /auth/login
   ├─ Valida DTO
   ├─ Busca user no DB
   ├─ Verifica password (bcrypt)
   ├─ Gera JWT token
   └─ Retorna { access_token, user }
   ↓
4. Frontend: 
   ├─ Armazena token no Zustand
   ├─ Configura header Authorization
   └─ Redireciona para dashboard
```

### Create Live Flow
```
1. User preenche form de criação
   ↓
2. Frontend: apiClient.createLive(data)
   ├─ Adiciona Authorization header
   └─ POST /lives
   ↓
3. Backend:
   ├─ JwtGuard valida token
   ├─ ValidationPipe valida DTO
   ├─ LivesService.create()
   │  ├─ Insere no DB
   │  └─ Armazena no Redis cache
   └─ Retorna Live criada
   ↓
4. Frontend:
   ├─ Atualiza useLiveStore
   ├─ Exibe toast de sucesso
   └─ Atualiza LiveList
```

### Watch Multiple Lives Flow
```
1. User seleciona uma Room
   ↓
2. Frontend: 
   ├─ Carrega lives IDs da room
   ├─ Para cada ID, busca dados da live
   ↓
3. Backend: GET /lives/:id (várias requisições)
   ├─ JwtGuard passa
   ├─ Verifica Redis cache
   ├─ Se não found: busca no DB
   ├─ Armazena em cache
   └─ Retorna Live data
   ↓
4. Frontend:
   ├─ Armazena no useLiveStore
   ├─ Renderiza MultiPlayer com React Player
   └─ Grid responsivo se ajusta
```

## Padrões de Comunicação

### Request/Response
```
Frontend Request:
{
  method: "POST",
  url: "http://localhost:3000/auth/login",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer token" // se protegido
  },
  data: {
    email: "user@example.com",
    password: "password123"
  }
}

Backend Response:
{
  status: 200,
  data: {
    access_token: "eyJhbGciOiJIUzI1NiIs...",
    user: {
      id: "uuid",
      email: "user@example.com",
      username: "johndoe"
    }
  }
}
```

## Segurança

### Layers de Segurança
```
┌──────────────────────────────────┐
│ 1. Network Layer                 │ HTTPS/TLS
├──────────────────────────────────┤
│ 2. Authentication Layer          │ JWT Tokens
├──────────────────────────────────┤
│ 3. Authorization Layer           │ JwtGuard
├──────────────────────────────────┤
│ 4. Validation Layer              │ Class-Validator
├──────────────────────────────────┤
│ 5. Password Security             │ bcrypt (10 rounds)
├──────────────────────────────────┤
│ 6. CORS Configuration            │ Whitelist origins
├──────────────────────────────────┤
│ 7. Input Sanitization            │ DTOs + validation
│                                  │
└──────────────────────────────────┘
```

## Escalabilidade

### Horizontal Scaling
```
Load Balancer
     ↓
┌────┴─────┐
│    │     │
v    v     v
[API1][API2][API3]  Backend instances
     │    │    │
     └────┴────┘
         ↓
   [PostgreSQL Master]
         ↓
   ┌─────┴──────┐
   v            v
 [Slave1]   [Slave2]  Replication
   
   [Redis Cluster]  Distributed cache
```

### Performance Optimization
- Redis cache para queries frequentes
- Paginação em endpoints que retornam múltiplos dados
- Lazy loading de componentes React
- Code splitting automático (Next.js)
- Compression de responses

## Deployment Architecture

```
┌─────────────────────────────────────────────┐
│ Docker Compose (Development)                │
├─────────────────────────────────────────────┤
│                                             │
│ [nginx] → [Backend] → [PostgreSQL]          │
│    ↓          ↓                             │
│ [Frontend] [Redis]                          │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Kubernetes (Production - Optional)          │
├─────────────────────────────────────────────┤
│                                             │
│ [Ingress] → [Service]                       │
│              ↓                              │
│         [Backend Pod x3]                    │
│         [Frontend Pod x2]                   │
│         [PostgreSQL StatefulSet]            │
│         [Redis StatefulSet]                 │
│                                             │
└─────────────────────────────────────────────┘
```

---

**Arquitetura robusta e escalável para Multi Lives! 🚀**
